__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
/dist/
/build/
.venv/
venv/
.env

# JIT cache produced by examples / tests
.cache/
*.o
*.so
*.a
*.stamp

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# xmake (no longer used; kept gitignored if anything appears locally)
.xmake/

# Vendored C++ deps (boost / highway / RMM / spdlog) — fetched by
# scripts/populate_vendor.py at install time / wheel build.
src/srdatalog/runtime/vendor/

# Wheel build cache
wheelhouse/
