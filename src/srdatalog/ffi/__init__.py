'''FFI helpers: C header generation + cffi-based Python wrapper.

Formed from the former ``ffi_header.py`` + ``ffi_wrapper.py`` top-level
modules. Old imports like ``from srdatalog.ffi.header import …`` are
now ``from srdatalog.ffi.header import …``.
'''
